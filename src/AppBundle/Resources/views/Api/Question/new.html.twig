{% extends '::base.html.twig' %}

{% block body -%}
    <div class="container">
        <div class="card">
            <h1>Nouvelle question</h1>

            {{ form(form) }}

            <a href="{{ path('admin_question') }}" class="btn btn-primary">
                Back to the list
            </a>
        </div>
    </div>
{% endblock %}
